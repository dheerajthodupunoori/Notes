NUMPY Package:
---------------
used for mathematical operations , mainly arrays
Functions:
----------
1.numpy.floor(numpy_array)
2.numpy.ceil(numpy_array)
3.numpy.rint(numpy_array)
4.numpy.sum(numpy_array,axis)-by default axis is Zero , returns the sum all the elements.
5.numpy.prod(numpy_array,axis)-by default axis is Zero , returns the sum all the elements.
6.numpy.min(numpy_array , axis) - gives the minimum number along that axis.
7.numpy.max(numpy_array ,axis)- gives the maximum value along the specified axis .
			
			axis - 0 -> column wise
		    axis - 1 -> row wise 

https://jakevdp.github.io/PythonDataScienceHandbook/02.04-computation-on-arrays-aggregates.html (for axis)

8.numpy.mean(numpy_array , axis) - computes the mean along the specified axis 
9.numpy.var(numpy_array, axis) - computes the variance along the specified axis.
10.numpy.std(numpy_array,axis)-computes the standard deviation along the specified axis.









Notes:
------

numpy.set_printoptions(parameter) --> display options for numpy objects --> while priniting or displaying anu numpy object , 
if we want to print in any specific order or if we want to customize the displaying of elements we can make use of this.
(https://docs.scipy.org/doc/numpy/reference/generated/numpy.set_printoptions.html)

----------------------------------------------------------------------------------------------------------------------
